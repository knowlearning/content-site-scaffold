```sh
# install with pnpm for performance
pnpm install

# run the development server
npm run dev
```

Open the link (accept the security warning) and you can
create content that can be embeded with a dashboard.